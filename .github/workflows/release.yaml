name: Release
on:
  workflow_dispatch:
    
jobs:
  release:
    uses: sjexpos/ecomm-cicd-actions/.github/workflows/maven-releasing.yaml@main
    with:
      group_id: io.oigres.ecomm
      artifact_id: users-service-spring-boot
